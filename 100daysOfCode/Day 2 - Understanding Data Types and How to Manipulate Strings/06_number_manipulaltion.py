#Rounding numbers:
print(round(8 / 3))

#Rounding numbers to a specified decimal house:
print(round(8 / 3, 2))

#Floor division. If you want a division to produce an integer instead of a floating point number:
print(8 // 3)

#f-string, in front of a string, we type the character f in front of the string, before the f"text" and add the variable we want to show within the the string with curly braces: 

score = 100
height = 1.8
isWinning = True
print(f"Hello John, your score is: {score}, your height is {height}, and 'you're winning' is equals to: {isWinning}")

